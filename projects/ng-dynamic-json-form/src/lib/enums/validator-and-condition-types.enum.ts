export enum ValidatorAndConditionTypes {
  HIDDEN = 'hidden',
  DISABLED = 'disabled',
  REQUIRED = 'required',
  REQUIRED_TRUE = 'requiredTrue',
  MIN = 'min',
  MAX = 'max',
  MIN_LENGTH = 'minLength',
  MAX_LENGTH = 'maxLength',
  EMAIL = 'email',
  PATTERN = 'pattern',
  CUSTOM = 'custom',
}
