export enum ValidatorAndConditionEnum {
  hidden = 'hidden',
  disabled = 'disabled',
  required = 'required',
  requiredTrue = 'requiredTrue',
  min = 'min',
  max = 'max',
  minLength = 'minLength',
  maxLength = 'maxLength',
  email = 'email',
  pattern = 'pattern',
  custom = 'custom',
}
