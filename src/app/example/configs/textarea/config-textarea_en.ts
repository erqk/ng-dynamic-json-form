import { FormControlConfig } from 'ng-dynamic-json-form';

export const CONFIG_TEXTAREA_EN: FormControlConfig = {
  label: 'Textarea',
  formControlName: 'textarea',
  type: 'textarea',
  props: {
    rows: 5
  }
};
